import { Navigate } from "react-router-dom";
document.title = "Blue Berry | Home";

const Home = () => {
document.title = "Blue Berry | Home"

	return <Navigate to="/events"/>
};

export default Home;
